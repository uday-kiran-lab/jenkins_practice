pipeline {
    agent {
        docker{image 'python:3.12-alpine'}
    }

    stage{
        steps('python version'){
            sh 'python --version'
        }
        steps('insatll python packages'){
            sh 'python3 hello.py'
        }
    }
}